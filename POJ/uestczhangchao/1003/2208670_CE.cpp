#include<iostream.h>
int main()
{float length;
float len=0;
int i;
int count=0;
cin>>length;
if(length!=0.00)
{
for (i=2;i<len;i++)
{
len=len+1.00/i;
cout++;
}
}
if(length>=0.01&&length<=5.2)
cout<<count<<"card(s)"<<endl;
}